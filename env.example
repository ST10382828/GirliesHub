# ============================
# GirliesHub Environment Setup
# ============================
# Notes:
# - Avoid trailing slashes in URLs (Render/Vercel/Netlify will trip on CORS + URL joins).
# - For production, set these as platform env vars (Render/Netlify). Local dev can use .env files.
#

# ----------------------------
# Backend (Render) - REQUIRED
# ----------------------------
NODE_ENV=production
PORT=5001

# CORS: set to your Netlify site URL (NO trailing slash)
# Example: https://grilieshub.netlify.app
CORS_ORIGIN=
# (Alias supported): ALLOWED_ORIGIN=

# Firebase Admin (backend)
FIREBASE_PROJECT_ID=girlieshub-cb344
# Paste the *minified JSON* (one line) in production.
FIREBASE_SERVICE_ACCOUNT_JSON=

# AI
GEMINI_API_KEY=

# Optional: enable description encryption (64 hex chars = 32 bytes)
ENCRYPTION_KEY=

# Blockchain (Awakening)
BDAG_RPC_URL=https://rpc.awakening.bdagscan.com
PRIVATE_KEY=
CONTRACT_ADDRESS=

# Queue worker
# If true, the API server will also process the Firestore->chain queue.
RUN_QUEUE_WORKER=true

# Optional: if true, writes to chain immediately on request submit (otherwise queued)
DUAL_WRITE_MODE=false

# ----------------------------
# Frontend (Netlify) - REQUIRED
# ----------------------------
# API base URL (your Render backend URL, NO trailing slash)
# Example: https://girlieshub-api.onrender.com
REACT_APP_API_URL=

# Blockchain config (frontend wallet connect)
REACT_APP_BDAG_RPC_URL=https://rpc.awakening.bdagscan.com
REACT_APP_BDAG_CHAIN_ID=1043
REACT_APP_BLOCKDAG_EXPLORER=https://bdagscan.com/awakening
REACT_APP_CONTRACT_ADDRESS=

# Frontend Firebase (if you were using env vars on Netlify)
REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_PROJECT_ID=girlieshub-cb344
REACT_APP_FIREBASE_STORAGE_BUCKET=
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=
REACT_APP_FIREBASE_APP_ID=
